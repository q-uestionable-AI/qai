---
title: Research Management
description: Browse runs, findings, and targets across all modules
---

<Note>
This section documents the q-ai web UI, which is under active development.
Content will be added as the web UI stabilizes.
</Note>

## Overview

The research view provides a cross-module browser for all data in the q-ai database — runs, findings, and registered targets. It mirrors the data available via `qai runs list`, `qai findings list`, and `qai targets list` in a filterable, sortable interface.